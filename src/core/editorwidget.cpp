#include "core/editorwidget.h"

namespace Slick {

    namespace Core {

        EditorWidget::EditorWidget(QWidget* parent) :
            QWidget(parent),
            m_editor(nullptr)
        {
        }

    }

}
